# 되돌리기 안내 (ROLLBACK)

2026년 9월 리디자인 작업의 복구 방법입니다.
**되돌릴 일이 생기면 이 문서만 보시면 됩니다.**

---

## 복구 기준점

| 항목 | 값 |
|---|---|
| 리디자인 직전 커밋 | `01241d3` — "feat: SEO/GEO 최적화 - 구조화 데이터, 메타데이터, 사이트맵 개선" |
| 작업 브랜치 | `claude/peace-church-site-redesign-aa30n9` |

`01241d3`이 **지금까지 잘 쓰고 계시던 그 화면**입니다. 이 커밋은 삭제되지 않고
브랜치 히스토리에 그대로 남아 있습니다.

---

## 방법 1 — Vercel에서 즉시 되돌리기 (가장 빠름, 1분)

코드를 건드리지 않고 배포만 이전 버전으로 되돌립니다.

1. Vercel 대시보드 → 이 프로젝트 → **Deployments**
2. 리디자인 이전 배포(커밋 `01241d3`)를 찾습니다
3. 오른쪽 `…` 메뉴 → **Promote to Production**

끝입니다. 사이트가 즉시 예전 화면으로 돌아갑니다.
데이터베이스는 건드리지 않으므로 설교·묵상·댓글은 그대로입니다.

---

## 방법 2 — 코드까지 되돌리기

```bash
git checkout claude/peace-church-site-redesign-aa30n9
git revert --no-commit 01241d3..HEAD
git commit -m "리디자인 되돌리기"
git push origin claude/peace-church-site-redesign-aa30n9
```

`revert`는 커밋을 지우지 않고 "반대 방향 커밋"을 새로 쌓는 방식이라,
나중에 다시 리디자인으로 돌아오고 싶을 때도 기록이 남아 있습니다.

---

## 일부만 되돌리고 싶다면

전부가 아니라 특정 부분만 마음에 들지 않는 경우입니다.

| 되돌리고 싶은 것 | 고칠 파일 |
|---|---|
| **색 전체** (남빛·금빛 → 예전 코발트) | `tailwind.config.ts` 의 `primary` / `gold` 값만 예전 값으로 |
| 히어로·홈 구성 | `app/page.tsx` |
| 상단 메뉴 | `components/Header.tsx` |
| 모바일 하단 탭바 | `app/layout.tsx` 에서 `<MobileTabBar />` 한 줄 삭제 |
| 새 페이지 3개 | `app/visit`, `app/prayer`, `app/news` 폴더 삭제 + 메뉴 링크 정리 |

색은 클래스 이름(`primary-700` 등)을 바꾸지 않고 **값만** 교체했기 때문에,
`tailwind.config.ts` 하나만 되돌리면 사이트 전체 색이 예전으로 돌아갑니다.

---

## 데이터베이스에 대하여

이번 작업은 **기존 표를 하나도 수정하지 않았습니다.**

- 그대로 둔 표: `Sermon`, `Comment`, `Devotional`, `DonationInfo`
- 새로 추가한 표: `PrayerRequest`(기도의 벽), `News`(평안소식)

따라서 코드를 되돌려도 설교·묵상·댓글 데이터는 아무 영향을 받지 않습니다.
새 표를 완전히 없애고 싶으시면 `prisma/schema.prisma`에서 두 모델을 지운 뒤
`npx prisma db push`를 다시 실행하시면 됩니다. (그 표에 쌓인 기도제목·소식은 함께 삭제됩니다.)

---

## 처음 배포할 때 해야 할 일

딱 두 가지입니다.

### 1. 관리자 비밀번호 정하기 (필수)

Vercel 대시보드 → 프로젝트 → **Settings** → **Environment Variables** 에서
아래를 추가하고 재배포합니다.

```
ADMIN_PASSWORD = 원하시는 비밀번호
```

이 값을 설정하기 전까지 `/admin`은 **잠겨 있습니다**(안전을 위한 기본 동작).
로그인 화면이 이 안내를 그대로 보여줍니다.

비밀번호를 바꾸면 기존 로그인은 전부 자동으로 풀립니다.
로그인 상태는 7일간 유지됩니다.

### 1-2. 배치 실행 비밀키 정하기 (필수)

같은 곳에 아래도 추가합니다.

```
CRON_SECRET = 아무도 모르는 긴 문자열
```

Vercel 이 정해진 시각에 우리 주소를 부를 때 이 값을 함께 보냅니다.
설정하지 않으면 배치 호출이 거부되어 자동 등록이 동작하지 않습니다.

돌고 있는 배치는 두 개입니다.

| 언제 | 무엇 |
|---|---|
| 매일 오전 6시 | 오늘의 공동 기도제목을 기도의 벽에 등록 |
| 매주 일요일 오전 7시 | 이번 주 주보의 교회소식을 평안소식에 등록 |

주보 쪽은 읽어온 내용이 확실하지 않으면 올리지 않고 넘어갑니다.
그럴 때는 관리자 화면의 **주보 미리보기**에서 확인하고 직접 올리시면 됩니다.

### 2. 기도의 벽 · 평안소식 표 만들기

셋 중 **아무거나 하나만** 하시면 됩니다.

**방법 A — 관리자 화면에서 버튼 누르기 (가장 쉬움, 터미널 불필요)**

`/admin`에 로그인하면 맨 위에 "표 만들기" 버튼이 있는 안내가 뜹니다.
한 번 누르면 끝이고, 표가 만들어지면 그 안내는 사라집니다.
여러 번 눌러도 안전합니다.

**방법 B — Neon 콘솔에서 SQL 실행**

Neon 대시보드 → 해당 프로젝트 → **SQL Editor** 를 열고,
`prisma/manual-setup.sql` 파일 내용을 통째로 붙여넣어 실행합니다.

**방법 C — 내 컴퓨터에서 명령 실행**

Node.js가 설치된 컴퓨터에서:

```bash
git clone https://github.com/kelin1575/peacechurch
cd peacechurch
npm install

# Vercel 환경변수의 DATABASE_URL / DIRECT_URL 값을 .env 파일에 넣습니다
cp .env.example .env
# .env 를 열어 DATABASE_URL, DIRECT_URL 을 실제 값으로 수정

npx prisma db push
```

> **어느 방법이든 표를 만들기 전에도 사이트는 정상 동작합니다.**
> 기도의 벽과 평안소식 두 페이지가 "준비 중입니다" 안내를 보여줄 뿐,
> 설교·묵상·성경·찬송가는 그대로 열립니다.

---

## 관리자 로그인은 어떻게 동작하나요

- `/admin` 아래 모든 화면과 관리자용 API가 로그인 뒤에 있습니다.
- 성도들이 쓰는 기능은 로그인 없이 그대로 열려 있습니다 —
  댓글 달기, 기도제목 올리기, "함께 기도합니다", 소식·설교·묵상 읽기.
- 배치 실행용 `/api/daily-sync`는 예전처럼 자체 `secret` 파라미터로 보호됩니다.
- 비밀번호는 데이터베이스에 저장되지 않습니다. 환경변수 하나로만 관리하며,
  로그인하면 서명된 쿠키(httpOnly)가 발급됩니다.

되돌리고 싶으시면 `middleware.ts` 파일 하나를 삭제하면 예전처럼
로그인 없이 열리는 상태로 돌아갑니다. (권장하지 않습니다.)
